/**
 * Class defining global configuration values.
 */

'use strict';

export class Config {
    /**
     * Prefix to CSS project-specific
     * ids and classes to avoid name
     * collisions with other selectors
     * on a web page
     * @type {string}
     */
    static cssPrefix = 'su-';
}
